// метод INCLUDES возвращает true или false
// Он не возвращает индекс элемента, ему не важно сколько раз элемент встречается в массиве 

// Пример:
let test = [44, 55, 66, 77, 88, 2, 3, 4];
if (test.includes(77)) {
  console.log('yes');

} 
else {
  console.log('no')
}